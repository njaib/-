services:

- type: worker

  name: telegram-browser-ai-bot

  runtime: python


  buildCommand: |

    pip install -r requirements.txt

    playwright install --with-deps chromium



  startCommand:

    python main.py